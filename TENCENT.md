# 比奇堡工作台 · 腾讯云轻量服务器部署指南（傻瓜版）

> 目标：把工作台部署到腾讯云，任何网络、任何手机都能打开完整版（同步 / AI / 热点全可用），数据 24 小时在线、不丢。

---

## 第一步：买一台轻量云服务器

1. 打开 **腾讯云官网** → 搜索「轻量应用服务器」→ 点「立即选购」。
2. 地域：选离你近的（如「上海」或「广州」）。
3. 镜像：**系统镜像 → Ubuntu 22.04 LTS**（最简单，后续手动装 Docker）。
   - 也可以选「Docker 基础镜像」，那就跳过装 Docker 那一步。
4. 套餐（配置）：选 **2 核 2G** 内存，带宽 **4–6 Mbps** 即可。
   - 新用户常有「首单特惠」，年付约 **¥60–100/年**，很划算。
5. 购买时长：先选 **1 年**（到期可续）。
6. 设置「实例名称」随便填，如 `workbench`。
7. 设置 **root 密码**（一定要记住！后面要用），或用「自动生成密码」然后记下来。
8. 勾选同意，付款。

> 付完款后，在「轻量应用服务器」控制台能看到你的机器，记下两样东西：
> - **公网 IP**（一串数字，如 `129.226.x.x`）
> - **root 密码**（你刚设的）

---

## 第二步：把安装包传上去

我已经在本地给你打好包：`workbench-deploy.tar.gz`（已排除密钥和隐私数据）。

**方式 A（最省事，推荐）：交给我远程装**
把下面两样发给我，我直接 SSH 上去一键装好：
- 公网 IP
- root 密码

> 安全提示：这是你自己的服务器密码，只在这一次部署用；装完后你可以改密码。

**方式 B（你自己来）：手动上传 + 运行**
1. 在腾讯云控制台点你的实例 → 「登录」（网页终端）或用自己的终端 `ssh root@<公网IP>`。
2. 安装包上传：
   - 控制台有「文件上传」功能可直接拖 `workbench-deploy.tar.gz`；
   - 或本机终端运行：`scp workbench-deploy.tar.gz root@<公网IP>:/root/`
3. 在服务器终端解压并部署：
   ```bash
   cd /root
   tar -xzf workbench-deploy.tar.gz
   cd workbench-app
   bash deploy-cloud.sh
   ```

---

## 第三步：放通防火墙端口

腾讯云默认只开 22 端口，要手动放 8080：
1. 控制台 → 你的实例 → 「防火墙」→「添加规则」。
2. 应用类型选「自定义」，协议 **TCP**，端口 **8080**，策略「允许」，来源「全部」。
3. 确定。

---

## 第四步：打开并用起来

1. 浏览器访问 `http://<公网IP>:8080`，能看到工作台首页就成功了。
2. 进入「设置 → 云端访问令牌」，填入 `changeme123`（或你自定的令牌，需和部署时一致）。
3. 迁移数据：在 Mac 本地工作台「设置 → 导出备份」下载一个 json，再在云端「导入数据」。
   - 或把 Mac 上的 `data/state.json` 内容贴进云端的导入框。

---

## 第五步（可选）：做成手机全屏 App + HTTPS

iOS「添加到主屏幕」做成 PWA 需要 HTTPS。两种办法：
- **简单**：用腾讯云「宝塔面板」镜像重装，宝塔里点几下就能申请免费 SSL，反代到 8080，得到 `https://你的域名`。
- **或先不管**：用手机 Safari 把 `http://IP:8080` 加到主屏幕当书签，功能完全正常，只是不是全屏壳。

---

## 常见问题

- **打不开页面**：九成是防火墙没放 8080（看第三步）；其次确认 `deploy-cloud.sh` 最后一行显示部署完成。
- **容器挂了**：`docker restart workbench`；看日志 `docker logs workbench`。
- **数据在哪**：存在 Docker 卷 `workbench-data`，重装容器不会丢；整机重装系统才会没。
- **AI 用不了**：部署时没传 `AI_API_KEY`，在 `docker run` 里加 `-e AI_API_KEY=你的DeepSeekKey` 再 run 一次。
